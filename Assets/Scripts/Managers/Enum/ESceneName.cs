namespace Managers.Enum
{
    /// <summary>
    /// This enum represents scene in builder.
    /// Be sure that enum index is equal to its build index!
    /// (GameManager scene always has index equal to 0!)
    /// </summary>
    public enum ESceneName
    {
        GameManager = 0,
        Game = 1,
    }
}