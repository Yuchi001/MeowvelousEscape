namespace CatPackage
{
    /// <summary>
    /// Those enum values should always represent the exact name of stat in Tmpro Sprite and Assets settings
    /// </summary>
    public enum EStatType
    {
        attackSpeed,
        attackDamage,
        health,
        cooldown,
        level
    }
}